apiVersion: v1
kind: Service
metadata:
  name: ml-api-service
  namespace: ml-system
  labels:
    app: ml-api
spec:
  type: LoadBalancer
  selector:
    app: ml-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000
  sessionAffinity: None